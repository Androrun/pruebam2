-- CreateTable
CREATE TABLE "Reptil" (
    "id" SERIAL NOT NULL,
    "name" TEXT NOT NULL,
    "descrption" TEXT NOT NULL,
    "color" TEXT NOT NULL,

    CONSTRAINT "Reptil_pkey" PRIMARY KEY ("id")
);
